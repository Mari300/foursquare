$(document).ready();
var address = $("#address").val();
var foods = $("#foods").val();
 foods <= 0$(".=#btn btn-primary").click(function() {
             alert("Type your location and what you're craving to continue!");

        }
  
   // if both of the search bars don't have anything in them, alert the user to type something into both$(".btn btn-primary").click(     
}

    );


// dsjnd 